{
  "buildCommand": "prisma generate && (prisma migrate resolve --rolled-back 20260215114646_add_template_marketplace || true) && prisma migrate deploy && next build",
  "env": {
    "SKIP_ENV_VALIDATION": "true"
  }
}
